!
router bgp 100
  bgp default ipv4-vpn
  no bgp ebgp-requires-policy
  no bgp network import-check
  neighbor 100.0.1.1 remote-as 100
  bmp targets targ
    bmp listener 0.0.0.0 port 12345
  exit
exit
!
router bgp 100 vrf BLUE
  bgp default ipv4-vpn
  no bgp ebgp-requires-policy
  no bgp network import-check
  neighbor 100.0.2.1 remote-as 100
exit
!